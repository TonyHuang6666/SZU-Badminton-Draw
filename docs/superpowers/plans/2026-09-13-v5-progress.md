# v5.0 实施记录

用户于 2026-09-13 授权“按计划开始5.0”。产品基线为 v4.6.0（`4bc1c0f`），规划提交为 `a1d1049`；实施位于独立工作树 `.worktrees/v5-unified-workspace`、分支 `feature/v5-unified-workspace`。

| 任务 | 状态 | 证据 |
| --- | --- | --- |
| 1 .NET 10 / Persistence 工程 | 已完成并评审通过 | `e5fc801`；SDK 10.0.301；锁定还原、漏洞审计通过；Release 构建零警告/错误；140 项测试通过 |
| 2 领域模型与状态机 | 已完成并评审通过 | `207ec07`、`4e4366a`；54 项专项 / 194 项全量测试通过；重开清理、策略枚举及选手身份问题已修正 |
| 3 无时间的比赛关系图 | 已完成并评审通过 | `38b21cf`、`2322d3c`；共享拓扑、稳定图标识、结果投影；轮空节点边界已统一 |
| 4 统一 SQLite 工作区 | 已完成并评审通过 | `5f6144e`；26 项真实存档测试；双进程修订竞争与锁释放检查通过 |
| 5 工作区命令与自动保存 | 已完成并评审通过 | `2e40685`；真实存档自动保存、会话切换与提交后重读故障测试 |
| 6 Shell / 新建向导 | 已完成并评审通过 | `dde4416`、`6cbeb8b`；编辑器基线防止重新载入后覆盖外部修改 |
| 7 名单与公开抽签 | 已完成并评审通过 | 显式预览/导出/确认、部分导出与审计故障；`9018282` 补个人身份唯一校验；`c19d944` 补辅助表 A4 打印，三份真实导出副本经独立办公软件复验 |
| 8 统一调度器 | 已完成并评审通过 | `bf46396`、`e14881a`、`7168354`、`d404732`、`bfb8107`、`3bbeb9a`；全局约束、条件兼项路径、位置等价优化与有界容量预检 |
| 9 全局赛程板 | 已完成并评审通过 | `a421a5e` 编辑后端；`e0fc50c` 至 `4c2901a` 全局设置/看板及原生反馈修复；拖动、撤销、连锁、手动跨日已在 macOS 验证 |
| 10 赛果、材料与恢复 | 数据层、恢复界面、整批赛果导入和材料写入器已完成评审；材料包及现场执行页实施中 | 赛果后端 `09b198b`；恢复界面 `84a8dbf`/`392febe`；计分材料 `5374c01`、每日赛程 `e808f1a`、质量报告 `b5f92b7`/`ecdd39d`、绑定比赛身份的时间图 `a5bf848`；打印与单位修复 `9db79f9`/`01b1efd`/`c941845`，均经独立审查；完整材料包及现场执行页尚未完成 |
| 11 删除旧入口 | 待实施 | 等价 v5 功能通过验证后执行 |
| 12 端到端验收与发布准备 | 待实施 | 仅记录实际执行的平台和产物验证 |

## 实施调整

- 先引入聚合根引用的比赛图、资源和排程值类型，以便任务 2 能完整编译；图构造与调度算法仍在任务 3、8 实现。
- 任务 1 使用实际 SDK、依赖、构建和全量回归验证，省略仅重复断言常量等于 500 的测试；任务 4 直接测试 SQLite 的真实 schema 版本。
- 中间阶段先增加有真实后端的命令，调度、赛果、材料命令随相应任务接入，避免占位实现。
- 延续现有淘汰赛、循环赛和名次附加赛规则。循环赛排名转淘汰赛不属于本次新增范围；轮空不产生虚假的可编排比赛。
- 使用全赛事统一的最短休息和每日场次上限；项目可设置单场时长及淘汰赛分界时长，不带入隐藏的旧项目级每日编排限制。
- 每个阶段由独立代理进行代码评审；实施授权持续有效。最终合并、推送、发布遵循原计划的验收审阅步骤。
- 基础接口通过评审后，比赛图与 SQLite 存储在独立工作树并行实施，再串行整合，避免文件编辑冲突。
- 比赛图只包含实际需要进行的比赛；轮空直接晋级，不生成虚假比赛节点。外部传入的显式轮空节点在存档校验和显示投影处一致拒绝。
- 验收工具仍直接引用旧存档和多项目工作流，因此其实际 v5 迁移提前到任务 10 后、任务 11 删除前，避免删除后解决方案无法编译；最终产物和平台验收仍在任务 12 完成。
- 采用显式弃权标记和真实零用时；普通比赛缺比分或用时不隐式推进。仅导入未填赛果记录后仍允许无赛果重排，原处理记录明确作废并保留来源，不因此新增重排禁令。

评审门 A 已通过本地验证：全量 194 项测试、整套 Release 构建（零警告/错误）和依赖漏洞审计。远端 CI 尚未触发。

评审门 B 已通过本地验证（`5f6144e`）：锁定还原、依赖漏洞审计、整套 Release 构建（零警告/错误）、247 项全量测试。比赛图修复经独立复审通过；存储经独立评审通过。补充 macOS 双进程检查观察到同修订号仅一个提交、另一个修订冲突，以及持锁进程终止后等待进程可以继续保存。尚未执行 Windows 运行验证。

评审门 C 已通过本地验证（`6e520d0`）：锁定还原、依赖漏洞审计、整套 Release 构建（零警告/错误）、339 项全量测试（52 项 Desktop、287 项共享测试）。工作区命令、Shell、抽签后端和页面均经独立评审，阻断问题已修复并复审。抽签 Excel/PDF 小样检查修复了首轮标题被覆盖、女子单打标题以及中文列宽问题；程序生成的中文 PDF 已检查。独立办公软件在修复后的重渲染、原生界面和 Windows 验收仍待任务 12，不以 ViewModel 测试代替。种子取消勾选仍需清空序号的轻微交互提示列入后续页面收尾。

评审门 D 已通过本地验证（`c19d944`）：锁定还原、包含传递依赖的漏洞审计、整套 Release 构建零警告/错误、539 项全量测试（88 项 Desktop、451 项共享测试）。调度器、性能改动、原子编辑后端、全局设置和看板均经独立审查，无未解决的阻断项。只增加了与生产 Avalonia 同版的私有 Headless 测试依赖，用实际布局边界验证定位，而非仅断言焦点状态。

macOS 原生验收使用真实工作流生成的 19 场三项目存档副本：单场预览不改文件、确认保存、连锁两场预览/取消/确认、不可用场地拒绝、合法拖动自动保存、撤销、手动跨日和 70% 缩放定位均实际检查。发现并修复首次打开未自动加载移动基准、拖动反馈过期、成功保存提示被覆盖和重复滚动导致目标不可见；最终编译副本逐项复测通过。撤销后通过只读 SQLite 对比确认所有 19 个位置精确恢复。Windows、实际拖动悬停跨日期标签、物理打印和完整平台矩阵仍待最终验收。

公开抽签阶段补充原生验收已完成导入后显式预览、预览导出、确认、不生成赛程、退出重开；修复保存扩展名重复和导入后种子编辑不可用。独立 LibreOffice 重算三份抽签 Excel 副本并渲染全部九页，辅助表修复后每本为三页，中文、完整审计值及九列名单可读。此批抽签文件不含公式，不能代替后续赛果公式验收。

调度性能记录是单次本地观测，不是跨平台性能承诺：同一 80 场测试从约 31.0 秒优化至 6.03 秒，位置快照 SHA-256 始终为 `2dd9d6fe73be5ab9e0cf5608c70fb2b439d6aeacfe518739531c72a0956977ac`，完整硬约束验证为零违规。原 345 场、五日、每日四场上限的样例已证明存在同一选手至少 21 场兼容路径，超过总容量 20；有界必要条件检查在 89 毫秒返回容量不足，而不是返回部分成功。该大样例是安全拒绝案例，不是大规模成功排程；另一个明确可行的大规模成功案例仍需最终验收。

本记录随实施更新。尚未执行的测试或平台检查不视为通过。

任务 10 的已整合前置部分通过本地验证（`66d551e`）：661 项全量测试（573 项共享、88 项 Desktop）通过，Release 构建零警告/错误；最近一次依赖漏洞审计在 `9af04da` 前的同依赖版本上通过，后续未改变依赖。数据层完整保存赛果种类、实际比赛日、处理覆盖、导入来源和连续更正历史；弃权必须明确标记且真实用时为零。

健康恢复绑定当前修订、同赛事备份和预览会话；损坏恢复绑定实际损坏字节及选定备份，可在打开失败、没有可读会话时执行。恢复与审计同次发布，恢复后清除旧编辑与撤销状态。故障测试覆盖备份复制后才报错、部分副本、已提交后重读失败、手动备份成功但审计失败，并区分真实副本路径与已验证可用的备份。

仅导入未填赛果记录后，重抽或重排仍然允许。成功时保留原记录的来源、哈希和行证据，并关联同次操作审计标记作废；失败时正式文件和处理覆盖不变。

任务 10 的赛果导入命令已通过独立审查（`09b198b`）：23 项真实工作流／记录表／SQLite 新用例、135 项组合专项通过；最后一次完整回归为 **790 项**（702 项共享、88 项 Desktop），无失败或跳过，Release 构建零警告/错误；锁定还原与包含传递依赖的漏洞审计通过。预览不写文件，提交重新核对会话、修订、完整源内容及文件哈希，更正绑定明确的前后值和人工原因；成功整批一次保存，重复文件不产生修订、备份或通知。保存前失败与提交后重读失败分别保留真实文件及状态。

独立审查发现并修复 Excel 数组公式成员被误判为普通值、空数值公式缓存被误判为未填输入两处问题。五份实际导出文件在独立 LibreOffice 副本中重算，32 个公式无错误，胜者／负者后续对阵、跨日期来源、同名项目比赛、带嵌套括号的姓名及字面量公式样式姓名均匹配预期，隐藏标识和下拉校验保留，原文件哈希未变。五份 PDF 共五页均已实际渲染检查，中文字体内嵌且带 ToUnicode；记录表 A4 横向较密，不作为手写计分纸或物理打印验收。正式计分材料及现场页仍在实施，Windows／Microsoft Excel 验证仍待最终验收。

任务 10 材料写入器和恢复界面的最新整合验证（`c941845`）：**938 项全量测试**（796 项共享、142 项 Desktop）通过，无失败或跳过；整套 Release 构建零警告/错误，锁定还原与包含传递依赖的漏洞审计通过。此数值不包含尚未整合的赛果导入面板或完整材料包实现。

恢复界面已用真实工作流生成的独立存档副本完成 macOS 原生检查：无可读会话时拒绝无效备份、损坏文件恢复、健康文件手动备份、明确原因及勾选后恢复、关闭重开使旧预览失效。只读 SQLite 检查确认恢复审计、修订推进、损坏前像／健康恢复前像完整保留，选定来源备份和另一赛事文件未变；恢复时不生成虚假赛程或赛果。异步旧回调及提交后重读失败另由真实故障／Headless 测试覆盖。

材料写入器完成了真实产物验证，尚不等于完整材料包通过验收：六份个人计分 PDF 和四页团体计分表已逐页查看；每日赛程六个样例共 21 页已查看，其中 16 个场地按每页最多四个场地分组，避免缩成不可读细字。五份质量报告共 39 页已全部查看，未知条件负荷不会显示为零，非法赛程明确禁止普通材料导出；总览用 A4，详细负荷和来源表用 A3。八份时间图工作簿在独立办公软件副本中重算、渲染，共 33 页全部查看，中文、完整来源标识及实际比赛时间得到核对。所有批次保留原件，核验原件哈希，不把合成测试名单中的来源占位值当作真实导入文件哈希或已保存审计。

实际办公软件检查发现并修复了续页表头定义的兼容问题，以及对阵主表默认 Letter 而辅助表 A4 的不一致；团体／双打淘汰赛小组标题也改用正确的“队／对”。后者先复现三项失败，再通过专项与全量测试，并重新导出、检查团体样例四页。保留明确边界：Excel 的自然分页可能拆开淘汰赛连接线或合并单元格，循环赛续页只重复总标题而不重复内部列表表头，因此不将自动分页的 Excel 宣称为独立完整的打印对阵图。完整材料包将提供专门生成的 A4 对阵 PDF 用于打印，其真实产物验收仍待实施。

完整材料包的单次审计、全部文件先生成校验后发布、部分发布及保存失败的真实恢复信息，以及现场执行页的赛果导入和历史展示仍在实施。验收工具迁移将在该后端通过审查后执行，再删除旧入口。Windows／Microsoft Excel／物理打印检查尚未执行，远端 CI 尚未触发。

评审门 E 尚未通过，不代表完整现场流程或 5.0 可发布。
