## 5.0.0（发布说明草案）

5.0.0 重做了赛事工作流：先决定举办团体赛还是单项赛、包含哪些项目，以及本次只做公开抽签还是筹备完整赛事，再进入名单、抽签、统一排程和现场执行。本文是候选 Release 说明，正式发布前仍需完成远端 CI 与真实 Windows 门禁。

### 核心变化

- 一个 `.szbd` 工作区承载整场赛事；团体赛与单项赛不能混排，单项赛可包含一至五个项目。
- 新建时显式选择“仅公开抽签”或“完整赛事筹备”。导入名单不自动抽签，抽签也不自动排程。
- 抽签预览、预览导出、确认和正式导出分开，可配合公开线上会议完成抽签并在之后继续办赛。
- 单项目使用项目完成节奏；多项目由全赛事策略统一优化，所有项目共享比赛日、场地、裁判容量、休息和每日场次限制。
- 引入无时间比赛关系图、全局赛程板、未决胜负路径约束、容量预检、质量诊断，以及单场/连锁/跨日移动和撤销。
- 现场执行页统一赛果预览与导入、材料包导出、审计历史和健康/损坏存档恢复。
- 赛果更正需要明确确认和原因；重复、冲突、过期、畸形及来源不一致的数据不会静默覆盖工作区。
- 删除 4.x 的旧窗口、旧单项目调度、多存档合并和旧记录路径；Windows 与 macOS 共用 Avalonia 入口。

### 不兼容变化

- 5.0 工作区 schema 为 500，不读取或迁移 4.x 存档。
- 不再存在“先分别排项目、再合并多个存档”的操作方式。
- 团体赛和单项赛必须建立为不同赛事；需要查看旧赛事时继续使用对应旧版本。

### 本地候选验证

- 锁定还原、直接与传递依赖漏洞审计、Release 构建通过；构建为 0 警告、0 错误。
- 全量测试 1,077/1,077 通过；macOS 打包安全测试 11/11 通过。
- 七条真实工作流/规模验收全部通过，保留 586 个证据文件：公开抽签、单项目、多项目、团体赛、故障矩阵、292 场成功排程和 345 场安全拒绝。
- 44 个材料工作簿副本经 LibreOffice 重算；程序原始 PDF 26/26 页已查看。精确范围和机器检查/人工检查边界见验收报告。
- macOS ARM64 DMG 通过镜像与严格签名校验，并从只读 DMG 创建真实窗口；Windows x64 PE 候选包已交叉生成，但尚未真机运行。

完整证据见 [v5.0.0 本地候选验收报告](../acceptance/v5.0.0.md)。

### 已知边界

- Windows 真机、远端 GitHub Actions、Microsoft Excel、物理打印和完整 PDF 人工视觉矩阵仍待执行。
- macOS 候选包只有 ad-hoc 签名，尚无 Developer ID、公证或 stapling。
- 复杂淘汰图拼页可能切开连接线或单元格，宽记录表可能不适合手写；正式使用前必须打印预览。
- Linux 不在 5.0 正式验收矩阵内。
